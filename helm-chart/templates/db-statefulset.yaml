apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "visit-counter.componentName" (list . "web-db") }}   # DONE2
  labels:
    {{- include "visit-counter.labels" . | nindent 4 }}
    {{- include "visit-counter.selectorLabels" (list . "web-db") | nindent 4 }}   # DONE
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "visit-counter.selectorLabels" (list . "web-db") | nindent 6 }}   # DONE
  template:
    metadata:
      labels:
        {{- include "visit-counter.labels" . | nindent 8 }}
        {{- include "visit-counter.selectorLabels" (list . "web-db") | nindent 8 }}   # DONE
    spec:
      containers:
        - name: web-db
          image: postgres:18.3-alpine3.23
          envFrom:
            - secretRef:
                name: {{ include "visit-counter.componentName" (list . "web-db-secret") }}   # DONE2
          ports:
            - containerPort: {{ .Values.dbService.port }}   # FIX
              name: {{ .Values.dbService.portName }}   # FIX
          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - pg_isready -d "$POSTGRES_DB" -U "$POSTGRES_USER"
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - pg_isready -d "$POSTGRES_DB" -U "$POSTGRES_USER"
            initialDelaySeconds: 15
            periodSeconds: 20
          volumeMounts:
            - mountPath: /var/lib/postgresql/18/docker
              name: storage-volume
      volumes:
        - name: storage-volume
          persistentVolumeClaim:
            claimName: {{ include "visit-counter.componentName" (list . "web-db-pvc") }}   # DONE2
      initContainers:
        - name: busybox
          image: busybox:latest
          command: ["sh", "-c", "rm -rf /var/lib/postgresql/18/docker/lost+found"]
          volumeMounts:
            - mountPath: /var/lib/postgresql/18/docker
              name: storage-volume
