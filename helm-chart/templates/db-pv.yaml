apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Values.webAppDbPv.name }}   # DONE
spec:
  volumeMode: Filesystem
  storageClassName: standard
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /data/web-app-db-postgres/
    type: DirectoryOrCreate
