{{- if .Values.db.enabled }}
apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ include "visit-counter.componentName" (list . "web-db-pv") }}
spec:
  volumeMode: Filesystem
  storageClassName: standard
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /data/web-db-postgres/
    type: DirectoryOrCreate
{{- end }}
