global:
  stage: prod

web:
  nodeSelector:
    role: web-app
  replicas: 1
  image:
    tag: v4
  service:
    type: NodePort
    nodePort: 32080
db:
  enabled: false

aws:
  parameterStoreRegion: us-east-1

autoscaling:
  enabled: true

syntheticTraffic:
  enabled: false
