apiVersion: v1
kind: Service
metadata:
  name: db-svc
  labels:
    app: db-svc
spec:
  type: ClusterIP
  selector:
    app: web-app-db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: db-svc
